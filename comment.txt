Confirmed working end-to-end — popup UI shows the attach button correctly, "ยกเลิก" correctly discards the draft (no test note leaked into real task data), backend upload endpoint returns 201 with a working file path, and the static route serves the file back correctly.

สรุปฟีเจอร์แนบไฟล์ในหมายเหตุ EvalForm — เสร็จแล้ว:

Backend:
- database/schema.sql + DB จริง — เพิ่ม attachment_path, attachment_name ใน evaluation_scores
- backend/routes/shared/uploads.ts + controllers/shared/uploads.controller.ts — endpoint ใหม่ POST /api/uploads/attachment เก็บไฟล์ที่ backend/uploads/attachments/ (ตั้งชื่อไฟล์แบบ UUID กัน path traversal/ชื่อชนกัน) รับได้ทุกประเภทไฟล์ จำกัดขนาด 15MB
- backend/uploads/ ใส่ไว้ใน .gitignore แล้ว (ไฟล์ที่อัปโหลดจะไม่ถูก commit)

Frontend:
- ป๊อปอัพหมายเหตุใน EvalForm มีปุ่ม "แนบไฟล์ (รูปภาพ/เอกสาร)" ใต้ช่องข้อความ อัปโหลดทันทีตอนเลือกไฟล์ มีปุ่มลบไฟล์ที่แนบได้
- แสดงลิงก์ "ไฟล์แนบ" ในตารางสรุปผลทั้งหน้า preview หลังประเมินเสร็จ และหน้าประวัติ (/evaluations/[id])

ตรวจสอบแล้ว: tsc ผ่านทั้ง 2 ฝั่ง, next build ผ่าน, ทดสอบ endpoint จริงผ่าน API เห็นไฟล์ถูกเซฟและดึงกลับมาได้จริง